 
import 'package:flutter/material.dart';

class TrendingDestinationsScreen extends StatelessWidget {
  const TrendingDestinationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Trending Destinations')),
      body: const Center(child: Text('Trending Destinations Screen')),
    );
  }
}
