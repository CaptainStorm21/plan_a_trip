 
import 'package:flutter/material.dart';

class DestinationFinderScreen extends StatelessWidget {
  const DestinationFinderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Destination Finder')),
      body: const Center(child: Text('Destination Finder Screen')),
    );
  }
}
